<view class="li">
    <view
        class="a {{ visibility === filterType ? 'selected' : '' }}"
        data-filter="{{ filterType }}"
        bindtap="onChangeFilter"
    >
        {{ filterType }}
    </view>
</view>
